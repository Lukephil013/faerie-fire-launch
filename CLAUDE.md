# Claude Bootstrap

Read `AGENTS.md`, then `docs/HANDOFF.md`, before inspecting code. Run only the
narrow context command named by the handoff. Do not scan README, FEATURES,
design documents, or devlogs unless the task explicitly needs history.
