Drop the main background image here as:

    forest-ruins-main.jpg   (or .png / .webp)

This is the "cathedral ruins + light beam" reference image (arched stone hall,
vine-covered window, single light shaft) — the one dimmed background used
behind the Faerie Fire memory.html UI, with the glowing-mote layer on top.

Once the file is here, tell Claude and it'll wire it into the app.
